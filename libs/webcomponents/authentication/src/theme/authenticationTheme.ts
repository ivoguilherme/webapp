import { createTheme } from '@material-ui/core/styles';
import { palette, typography, components } from './props';

export const authenticationTheme = createTheme({
  palette,
  typography,
  components,
});
