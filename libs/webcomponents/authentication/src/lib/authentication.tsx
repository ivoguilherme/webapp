import styled from 'styled-components';

export function Authentication() {
  return (
    <div>
      <h1 data-testid="login-title">Welcome to Authentication!</h1>
    </div>
  );
}

export default Authentication;
